% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/system_of_equations.R
\name{expand_dummies}
\alias{expand_dummies}
\title{Expand Indexed Dummy-Variable Shorthand}
\usage{
expand_dummies(equations)
}
\arguments{
\item{equations}{A character vector of equation strings.}
}
\value{
A character vector of equations with every \code{dummies(...)} call
expanded.
}
\description{
Rewrites \code{dummies(prefix, spec)} calls into an additive series of plain
variable names \code{prefix_1+prefix_2+...}, where the indices come from
\code{spec} (parsed by \code{parse_index_spec()}, the same "single value or range,
comma-separated" grammar used by lag notation). For example,
\code{dummies(covid, 1:8)} becomes \code{covid_1+covid_2+...+covid_8}.

This runs before any other equation processing (priors, settings,
validation), so the expanded terms are indistinguishable from terms the
user typed by hand for every downstream step - including
\code{validate_completeness()}, which will require each expanded name (e.g.
\code{covid_1}) to be declared in \code{exogenous_variables} like any other
regressor. Unlike lagged variables, there is no separate "base variable"
backing a dummy family to validate instead, so no such exemption exists.

Any \code{dummies(...)} call is matched loosely first (so a malformed one is
actually caught here, rather than left untouched to fail later as an
unhelpful generic "invalid variable"), then the prefix and spec are
validated strictly, raising \code{cli::cli_abort()} with the offending call
shown verbatim if either is invalid.
}
\keyword{internal}
