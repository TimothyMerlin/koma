% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix_construction.R
\name{construct_beta_hat_j_matrix}
\alias{construct_beta_hat_j_matrix}
\title{Construct \eqn{\hat{\beta_j}}}
\usage{
construct_beta_hat_j_matrix(
  x_matrix,
  z_matrix_j,
  character_beta_matrix,
  jx,
  xbtxb = NULL
)
}
\arguments{
\item{x_matrix}{A \eqn{(T \times k)} matrix \eqn{X} of observations on
\eqn{k} exogenous variables.}

\item{z_matrix_j}{A \eqn{Z_j = y_j - Y_j * \gamma_j} matrix.}

\item{character_beta_matrix}{A matrix \eqn{\beta} that holds the
coefficients in character form for all equations. The dimensions of the
matrix are \eqn{(k \times n)}, where \eqn{k} is the number of
exogenous variables and \eqn{n} the number of equations.}

\item{jx}{The index of equation \eqn{j}.}

\item{xbtxb}{Optional precomputed \eqn{x_b'x_b}, where \eqn{x_b} is
\eqn{x_matrix} restricted to the columns kept for equation \eqn{j}. This
is invariant across Gibbs draws for a given equation, so callers that
iterate (e.g. the Gibbs sampler) can compute it once and pass it in to
avoid recomputing it on every call. If \code{NULL} (the default), it is
computed internally.}
}
\value{
\eqn{\hat{\beta_j}} with dimensions \eqn{k \times 1}.
}
\description{
\eqn{\hat{\beta_j} = (x_b'x_b)^{-1} x_b' Z_j}
with \eqn{x_b} being the predetermined variables and constant of equation
\eqn{j} and \eqn{Z_j = y_j - Y_j * \gamma_j}
}
\keyword{internal}
