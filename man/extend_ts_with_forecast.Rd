% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extend_ts_with_forecast.R
\name{extend_ts_with_forecast}
\alias{extend_ts_with_forecast}
\title{Extend Time Series List with Forecast Values at New Dates}
\usage{
extend_ts_with_forecast(ts_list, forecast_mts)
}
\arguments{
\item{ts_list}{A named list of original time series objects.}

\item{forecast_mts}{A multi-time series object with forecast values.
The column names should match the names in \code{ts_list}.}
}
\value{
A named list of time series objects, each extended with forecast
values for new dates only. For time series whose names are not present in
\code{forecast_mts}, the original time series is returned unmodified.
}
\description{
This function takes a named list of original time series and extends
each series with forecast values from a multi-time series (mts) only
at dates not already in the original time series.
}
\details{
The function will extend the time series in \code{ts_list} by appending
forecast values from \code{forecast_mts}. The extension will occur only for dates
that do not already exist in the original time series.
}
\keyword{internal}
