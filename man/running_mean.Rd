% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/running_mean.R
\name{running_mean}
\alias{running_mean}
\title{Running Means for koma_estimate Objects}
\usage{
running_mean(x, ...)
}
\arguments{
\item{x}{A \code{koma_estimate} object.}

\item{...}{Additional arguments controlling the output. See Details.}
}
\value{
A data.frame with columns \code{draw}, \code{value}, \code{variable}, \code{param},
\code{coef}, \code{draw_position}, \code{in_grace_window}, and \code{label}.
}
\description{
Compute running means (cumulative averages) for coefficient draws from a
\code{koma_estimate} object. By default, beta, gamma, and sigma draws are
included when available.
}
\details{
Additional arguments supported in \code{...}:
\describe{
\item{variables}{Optional character vector of endogenous variables to
include.}
\item{params}{Optional character vector of parameter groups to include
(e.g., "beta", "gamma", "sigma"). Defaults to all available.}
\item{thin}{Optional integer thinning interval for the stored draws.
Default is 1 (no thinning).}
\item{max_draws}{Optional integer cap on the number of draws returned.
When set, the most recent draws are kept.}
\item{grace_draws}{Optional integer number of initial retained draws to
flag as a grace window for convergence interpretation. If NULL, defaults
to \code{max(50, ceiling(0.1 * n_retained))} per series.}
}

Note: \code{sigma} values are based on \code{omega_tilde_jw} and use only
variances (no covariances) from each covariance draw.
}
