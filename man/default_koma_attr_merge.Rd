% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_series.R
\name{default_koma_attr_merge}
\alias{default_koma_attr_merge}
\title{Default merge behavior for a koma_ts attribute}
\usage{
default_koma_attr_merge(left, right, attr, op = NULL, template = NULL)
}
\arguments{
\item{left}{The first operand's attribute value, or \code{NULL} if absent.}

\item{right}{The second operand's attribute value, or \code{NULL} if absent.}

\item{attr}{Name of the attribute, used in the error message.}

\item{op}{Name of the arithmetic operation being performed (e.g. \code{"-"}),
used in the error message.}

\item{template}{Unused; accepted for signature compatibility with
handlers registered via \code{\link[=set_koma_attr_policy]{set_koma_attr_policy()}}.}
}
\value{
\code{left} (or \code{right}, when \code{left} is \code{NULL}) if the values match or
one side is absent. Errors otherwise.
}
\description{
Used by \code{\link[=merge_koma_attrs]{merge_koma_attrs()}} for any attribute without a policy
registered via \code{\link[=set_koma_attr_policy]{set_koma_attr_policy()}}. \code{NULL} operands are passed
through unchanged; otherwise the two values must be identical (per
\code{\link[base:all.equal]{base::all.equal()}}) or the merge errors, since there is no generic way
to combine two different metadata values (e.g. two different \code{anker}
anchors) without attribute-specific knowledge of what the operation
means.
}
\keyword{internal}
