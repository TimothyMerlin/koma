% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_identity_lag_collinearity}
\alias{validate_identity_lag_collinearity}
\title{Validate that Lagged Identities Are Not Collinear with Their Components}
\usage{
validate_identity_lag_collinearity(
  identities,
  predetermined_variables,
  call = rlang::caller_env()
)
}
\arguments{
\item{identities}{A list of identities, as returned by \code{\link[=get_identities]{get_identities()}}.}

\item{predetermined_variables}{A character vector of lagged variable
names, as returned by \code{\link[=parse_lags]{parse_lags()}}.}

\item{call}{The environment from which the error is called.}
}
\description{
An identity holds exactly (no error term), so for any lag \code{k},
\code{identity.L(k)} is an exact linear combination of \verb{component_1.L(k), ..., component_n.L(k)}. If \code{identity.L(k)} is used as a predetermined variable
in one equation while \emph{every} one of its components is \emph{also} lagged by
\code{k} somewhere else in the system, the resulting \code{x_matrix} is guaranteed
to be rank-deficient -- independent of the data. Because this only depends
on the equation specification (which variables are components of which
identity, and which lags are used where), it can be, and is, checked
eagerly here rather than deferred to estimation time.
}
\details{
Identities with dynamic (data-derived) weights are skipped: their weights
can vary over time, so the exact dependency cannot be guaranteed from the
specification alone. \code{\link[=validate_full_rank]{validate_full_rank()}} still catches those
numerically once data is available.
}
\keyword{internal}
