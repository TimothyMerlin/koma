% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{extract.koma_estimate}
\alias{extract.koma_estimate}
\title{Extract a texreg summary from a koma_estimate}
\usage{
extract.koma_estimate(
  model,
  variables = NULL,
  central_tendency = "mean",
  ci_low = 5,
  ci_up = 95,
  digits = 2,
  ...
)
}
\arguments{
\item{model}{A \code{koma_estimate} object.}

\item{variables}{Optional character vector of endogenous variables to
include. Defaults to all variables in \code{model$estimates}.}

\item{central_tendency}{Central tendency used when summarizing estimates
(e.g., "mean", "median"). Defaults to "mean".}

\item{ci_low}{Lower bound (percent) for credible intervals. Defaults to 5.}

\item{ci_up}{Upper bound (percent) for credible intervals. Defaults to 95.}

\item{digits}{Number of digits to round numeric values. Defaults to 2.}

\item{...}{Unused. Included for \code{texreg::extract()} compatibility.}
}
\value{
A \code{texreg} object when one variable is requested, otherwise a named
list of \code{texreg} objects.
}
\description{
Builds one or more \pkg{texreg} objects from a \code{koma_estimate}, so results
can be rendered with \code{texreg::screenreg()} or similar helpers.
}
\seealso{
\code{\link[=summary.koma_estimate]{summary.koma_estimate}} for
summary output with optional \pkg{texreg} formatting.
}
