% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes.R
\name{init_koma_theme}
\alias{init_koma_theme}
\title{Initiate Default Theme}
\usage{
init_koma_theme(
  index = list(start = NULL, end = NULL),
  title = list(font = NULL, text = NULL),
  font = NULL,
  trace_name = NULL,
  xaxis = list(tickfont = NULL, range = list(start = NULL, end = NULL)),
  yaxis = list(y = list(title = list(text = "QoQ, in \%"), tick_center = 0), y2 =
    list(title = list(text = "Level"), tick_center = 100), tickfont = NULL, number_ticks
    = 5),
  legend = list(font = NULL),
  color = list(marker = list(in_sample = "rgba(111,111,111,0.6)", forecast =
    "rgba(33,92,175,1)"), bar_textfont = list(in_sample = "black", forecast = "white"))
)
}
\arguments{
\item{index}{Optional list with two elements: \code{start} and \code{end}.
These dates are used to anchor the time series data. Dates should
be in the format: c(YEAR, QUARTER).}

\item{title}{A list to specify title options. Default is NULL.
\itemize{
\item \code{text}: Sets the plot's title. If not provided, then the variable name
will be displayed.
\item \code{font}: Sets the plot title font.
}}

\item{font}{Sets the global font. Note that fonts used in traces and other
layout components inherit from the global font.}

\item{trace_name}{Optional list to specify the naming of traces.
If not provided, the names will be generated based on the
start date of the forecasts. For instance, if the forecast
starts at 2023-Q1, the default name for \code{in_sample_growth}
would be "2023-Q1 Data (Growth Rate)", and similarly for
other traces.
The list can have four elements:
\itemize{
\item \code{in_sample_growth}: Naming for in-sample growth data traces.
\item \code{in_sample_level}: Naming for in-sample level data traces.
\item \code{forecast_growth}: Naming for forecast growth data traces.
\item \code{forecast_level}: Naming for forecast level data traces.
}}

\item{xaxis}{A list with custom labels for the x-axis.
\itemize{
\item \code{range}: Sets the range of this axis. List with \code{start} and \code{end}.
\item \code{tickfont}: Sets this axis' tick font, including tickfont for annual
growth rates.
}}

\item{yaxis}{A list with custom labels for the y-axes \code{y} (left) and
\code{y2} (right).
\itemize{
\item \code{y}: A list with custom labels for the y-axis.
\itemize{
\item \code{title}: Label for the left y-axis, defaults to "QoQ, in \%" (quarter
on quarter).
\item \code{tick_center}: Value to center the ticks around, defaults to 0.
}
\item \code{y2}: A list with custom labels for the y-axis.
\itemize{
\item \code{title}: Label for the left y-axis, defaults to "Level".
\item \code{tick_center}: Value to center the ticks around, defaults to 100.
}
\item \code{tickfont}: Sets this axis' tick font.
\item \code{number_ticks}: Integer for the desired number of ticks in the y-axis.
}}

\item{legend}{A list to specify legend options.
\itemize{
\item \code{font}: Sets the legend font.
}}

\item{color}{A list containing color options for different elements of the
plot.
When working with RGBA colors, the alpha value (the 'A' in RGBA)
determines the transparency of the color, ranging from 0 (fully
transparent) to 1 (fully opaque).
\itemize{
\item \code{marker}: A list specifying the colors for different traces.
\itemize{
\item \code{in_sample}: Color for in-sample data traces.
\item \code{forecast}: Color for forecast traces.
}
\item \code{bar_textfont}: A list specifying the text color for different bars.
\itemize{
\item \code{in_sample}: Text color for in-sample data bars.
\item \code{forecast}: Text color for forecast data bars.
}
}}
}
\description{
Initiate default theme used to plot forecasts.
}
