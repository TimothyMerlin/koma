% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_identification.R
\name{validate_full_rank}
\alias{validate_full_rank}
\title{Validate that x_matrix has full column rank}
\usage{
validate_full_rank(x_matrix, tol = 1e-07, call = rlang::caller_env())
}
\arguments{
\item{x_matrix}{A \eqn{(T \times k)} matrix \eqn{X} of observations on
\eqn{k} exogenous variables.}

\item{tol}{Numerical tolerance for detecting rank deficiency, passed to
\code{\link[base:qr]{base::qr()}} and used (relative to the largest singular value) to
identify the columns responsible.}

\item{call}{The environment from which the error is called.}
}
\description{
Detects exact linear dependence between columns of \code{x_matrix}, the
\eqn{(T \times k)} matrix of observations on exogenous and predetermined
variables used during estimation. Such dependence can arise, for example,
when a lagged identity variable (e.g. \code{gdp.L(1)}, where
\code{gdp == 0.5*manufacturing + 0.5*service}) is used alongside lags of its own
identity components elsewhere in the system: since an identity holds
exactly (no error term), its lag is then an exact linear combination of
other columns already in \code{x_matrix}. Left uncaught, this surfaces later as
an opaque \code{"computationally singular"} error from \code{Matrix::solve()} deep
inside the Gibbs sampler.
}
\keyword{internal}
