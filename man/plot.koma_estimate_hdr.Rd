% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.koma_estimate_hdr}
\alias{plot.koma_estimate_hdr}
\title{Plot HDRs for koma_estimate Objects}
\usage{
\method{plot}{koma_estimate_hdr}(x, y = NULL, ...)
}
\arguments{
\item{x}{A \code{koma_estimate_hdr} object.}

\item{y}{Ignored. Included for compatibility with the generic function.}

\item{...}{Additional arguments controlling the plot. See Details.}
}
\value{
A ggplot object, or a plotly object when \code{interactive = TRUE} and
plotly is available.
}
\description{
Plot HDR intervals for coefficients from a \code{koma_estimate_hdr} object.
Outliers beyond the outer HDR are shown if available in the object.
The plot requires HDR levels matching \code{box_levels} to be present in the
object.
}
\details{
Additional arguments supported in \code{...}:
\describe{
\item{variables}{Optional character vector of endogenous variables to plot.}
\item{params}{Optional character vector of parameter groups to plot
(e.g., "beta", "gamma", "sigma").}
\item{box_levels}{Optional numeric probabilities in (0, 1] (or \eqn{[0, 100]})
defining the inner and outer HDRs. By default, uses the available
HDR levels closest to 50\% and the maximum probability.}
\item{interactive}{Logical. If TRUE and plotly is available, return an
interactive plot via \code{plotly::ggplotly}. Default is FALSE.}
}
}
