% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{estimate}
\alias{estimate}
\alias{estimate.list}
\title{Estimate the Simultaneous Equations Model (SEM)}
\usage{
estimate(
  ts_data,
  sys_eq,
  dates,
  ...,
  options = list(gibbs = list(), fill = list(method = "mean")),
  estimates = NULL
)

\method{estimate}{list}(
  ts_data,
  sys_eq,
  dates,
  ...,
  options = list(gibbs = list(), fill = list(method = "mean")),
  estimates = NULL
)
}
\arguments{
\item{ts_data}{Time-series data set for the estimation.}

\item{sys_eq}{A \code{koma_seq} object (\link{system_of_equations}) containing details
about the system of equations used in the model.}

\item{dates}{Key-value list for date ranges in various model operations.}

\item{...}{Additional parameters.}

\item{options}{Optional settings for estimation. Use
\code{list(gibbs = list(), fill = list(method = "mean"))}. Elements:
\itemize{
\item \code{gibbs}: Gibbs sampler settings (see
\link[=get_default_gibbs_spec]{Gibbs Sampler Specifications}).
\item \code{fill$method}: "mean" or "median" used to fill ragged edges
during estimation.
}
See \link[=get_default_gibbs_spec]{Gibbs Sampler Specifications}.}

\item{estimates}{Optional. A \code{koma_estimate} object
(see \code{\link{estimate}}) containing the estimates of the previously
estimated simultaneous equations model. Use this parameter when some
equations of the system need to be re-estimated.}
}
\value{
An object of class \code{koma_estimate}.

An object of class \code{koma_estimate}is a list containing the following
elements:
\describe{
\item{estimates}{The estimated parameters and other relevant information
obtained from the model.}
\item{sys_eq}{A \code{koma_seq} object containing details about the system of
equations used in the model.}
\item{ts_data}{The time-series data used for the estimation, with any \code{NA}
values removed and lagged variables created.}
\item{y_matrix}{The Y matrix constructed from the balanced data, used in
the estimation process.}
\item{x_matrix}{The X matrix constructed from the balanced data, used in
the estimation process.}
\item{gibbs_specifications}{The specifications used for the Gibbs
sampling.}
\item{dates}{The date ranges used during estimation.}
\item{plain_ts_names}{Character vector of series names that were
supplied as plain \code{ts} (not \code{koma_ts}) in \code{ts_data}. These are assumed to
already be in rates and tagged accordingly; see the "Plain ts input"
section below.}
}
}
\description{
Estimate a system of simultaneous equations model (SEM) using a Bayesian
approach. This function incorporates Gibbs sampling and allows for
both density and point forecasts.
}
\details{
After estimation, use \code{\link[=summary.koma_estimate]{summary}} for a
full table of posterior summaries (with optional credible intervals and
texreg output) and \code{\link[=print.koma_estimate]{print}} for a concise
console-friendly overview of the estimated system.
}
\section{Parallel}{

This function provides the option for parallel computing through
the \code{future::plan()} function.
For a detailed example on executing \code{estimate} in parallel, see the vignette:
\code{vignette("parallel")}.
For more details, see the
\href{https://CRAN.R-project.org/package=future}{future package documentation}.
}

\section{Plain ts input}{

If any element of \code{ts_data} is a plain \code{ts} rather than a \code{koma_ts}
(see \code{\link{ets}}), it is assumed to already be in rates, the
form the model estimates on, and is converted to \code{koma_ts} with
\code{series_type = "rate"}, \code{method = "none"}: the values are used as-is, no
rate/level transformation is applied. A warning lists the affected
series, and the same list is stored in the returned object as
\code{plain_ts_names} (also surfaced when the \code{koma_estimate} is printed). If a
series actually needs to be converted from levels (e.g. via a percentage
or diff_log growth rate), convert it first with \code{\link{ets}} or
\code{\link{as_ets}}; see \code{vignette("koma-extended-timeseries")}.

\code{koma_ts} objects may carry custom attributes beyond \code{series_type}/
\code{method} (e.g. a project-specific \code{value_type}). Since all series in
\code{ts_data} must share the same attribute names (see \code{\link{as_mets}}),
any such extra attributes found on sibling \code{koma_ts} series are set to
\code{NA} on the converted series.
}

\section{Gibbs Sampler Specifications}{

\itemize{
\item \code{ndraws}: Integer specifying the number of Gibbs sampler draws. Default is
2000.
\item \code{burnin_ratio}: Numeric specifying the ratio for the burn-in period.
Default is 0.5.
\item \code{nstore}: Integer specifying the frequency of stored draws. Default is 1.
\item \code{tau}: Numeric tuning parameter for enforcing an acceptance rate.
Default is 1.1.
}
}

\examples{
data("simulated_sem")
set.seed(11)

fit <- estimate(
  ts_data = simulated_sem$ts_data,
  sys_eq = simulated_sem$sys_eq,
  dates = simulated_sem$dates,
  options = list(gibbs = list(ndraws = 10))
)
print(fit)

}
\seealso{
\itemize{
\item To create a \code{koma_seq} object see \code{\link{system_of_equations}}.
\item For a comprehensive example of using \code{estimate}, see
\code{vignette("koma")}.
\item Related functions within the package that may be of interest:
\code{\link{forecast}}.
}
}
